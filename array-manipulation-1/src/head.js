/* exported head */
// define a function called head that has one parameter, an array.
// return the value of the first index of the array.

function head(array) {
  return array[0];
}

// define a function named "head" that contains one argument, an array named "array".
// return the value of the first index of the array called as an arugment of the function named "head".
