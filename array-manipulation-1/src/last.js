/* exported last */
// define a function named "last" with one parameter named "array".
// return the value of the last index of the array called as an argument.

function last(array) {
  return array[array.length - 1];
}

// define a function named "last" with one parameter named "array".
// return the value of the array at the length of the array minus 1.
