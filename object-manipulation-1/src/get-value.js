/* exported getValue */
// define a function, 'getValue', with 2 parameters, 'object' and 'key'.
// return the key value of the object property.

function getValue(object, key) {
  var value = object[key];
  return value;
}

// define a function, 'getValue', with 2 parameters, 'object' and 'key'.
// assign the value of the key property of the object to the variable 'value'.
// return 'value'.
