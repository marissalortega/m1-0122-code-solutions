/* exported setValue */
// define a function 'setValue', with 3 parameters - 'object', 'key', and 'value'.
// assign the 'value' to the 'key' property of the 'object' object.

function setValue(object, key, value) {
  object[key] = value;
}

// define a function 'setValue', with 3 parameters - 'object', 'key', and 'value'.
// assign the 'value' to the 'key' property of the 'object' object.
