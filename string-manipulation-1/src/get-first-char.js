/* exported getFirstChar */
// define a function, "getFirstChar" with a single parameter, "string".
// return the value of the "string" parameter at its first index.

function getFirstChar(string) {
  return string.charAt(0);
}

// define a function, "getFirstChar" with a single parameter, "string".
// return the value of the first character by calling the charAt method of the "string" object.
