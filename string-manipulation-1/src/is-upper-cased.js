/* exported isUpperCased */
// define a function, 'isUpperCased', with a single parameter, 'word'.
// declare a variable that has the value of a boolean.
// loop through the string to see if it's true that all letters in the string are uppercase.
// return the value of the boolean.

function isUpperCased(word) {
  return word === word.toUpperCase();
}

// define a function, 'isUpperCased', with a single parameter, 'word'.
// return the expression 'word is strictly equal to the toUpperCase method of the word object".
